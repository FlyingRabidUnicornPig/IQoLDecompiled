/* Obfuscated as EDNENBIDEGJ in vanilla.dll
   Unsure if used or what it's use is, for potentially an unused
    menu animation thing? Unity.UI.Extensions.AccordionElement
*/

using System;

internal interface TweenThing
{
	void TweenValue(float NMJBJAOPJBP);

	bool ignoreTimeScale { get; }

	float duration { get; }

	bool ValidTarget();

	void Finished();
}